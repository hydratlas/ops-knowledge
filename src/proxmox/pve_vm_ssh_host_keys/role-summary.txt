このロールは、Proxmox VE環境で仮想マシンのSSHホストキーをvirtiofs共有ストレージに永続化し、VM再作成時にも同じホストキーを維持できるようにする機能を提供する。Ansibleタスクは、VMIDごとにvirtiofs共有内にディレクトリーを作成し、Ed25519形式の秘密鍵と公開鍵を配置する。VM側では、起動時にvirtiofs共有からホストキーを/etc/ssh/へコピーして適用することで、SSH接続時のホストキー警告を防止する。複数VMのキーを一元管理できるため、運用効率が向上し、キーのローテーションやバックアップも容易に実施できる。
